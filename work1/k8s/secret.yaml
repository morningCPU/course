# ── Secret（敏感配置，密码需 base64 编码）───────────────
# 编码命令：echo -n "your_password" | base64
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: default
type: Opaque
data:
  password: cmVkaXMxMjM=